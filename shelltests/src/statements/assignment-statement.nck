function main -> int do
  x: int;
  y: int;
  temp: int;
  
  x = 10;
  y = 20;
  
  printInt(x);
  printInt(y);

  temp = x;
  x = y;
  y = temp;

  printInt(x);
  printInt(y);

  return 0;
end
